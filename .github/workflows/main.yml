name: Update badges

on:
  schedule:
    # Runs at 2am UTC
    - cron: "10 * * *"
jobs:
  update-readme:
    name: Update Readme with badges
    runs-on: ubuntu-latest
    steps:
      - name: Badges - Readme
        uses: pemtajo/badge-readme@2.3.0
        with:       
          CREDLY_USER: khaled-waleed.a35d60a4 # optional, but default will use the same from github
